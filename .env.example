# ============================================================
# FlightCast — environment variables
# Copy to .env: cp .env.example .env
# Never commit .env to git.
# ============================================================

MARIADB_ROOT_PASSWORD=rootpw123
MARIADB_DATABASE=flightcast
MARIADB_USER=flightcast
MARIADB_PASSWORD=flightcastpw

# App container DB connection
DB_HOST=db
DB_PORT=3306
DB_NAME=flightcast
DB_USER=flightcast
DB_PASSWORD=flightcastpw

# Host port bindings
APP_PORT=8501
API_PORT=8000
